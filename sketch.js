//gif mzking variables
let canvas;
let gifLength=255*2;
const recording =false;



function setup() {
///////////////////////////////////////////////////////////////////////////
  var p5canvas = createCanvas(600,600);
  canvas = p5canvas.canvas;
///////////////////////////////////////////////////////////////////////////



///////////////////////////////////////////////////////////////////////////
  if(recording) capturer.start();
///////////////////////////////////////////////////////////////////////////
}

function drawSketch(){
  
}

// the draw function of p5
// advice:  write your code in drawSketch() instead
function draw() {
  drawSketch();
//gif making logic ////////////////////////////////////////////////////////
  if(recording){
    if(frameCount < gifLength){
      capturer.capture(canvas);
    } else if (frameCount === gifLength){
        capturer.stop();
        capturer.save();
    }
  }
///////////////////////////////////////////////////////////////////////////
}
